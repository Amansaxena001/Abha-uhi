import React from 'react';
import styles from './styles.module.scss';

interface IProps {}

const BasicNavbar: React.FC<IProps> = () => <div className={styles.outerContainer} />;

export default BasicNavbar;
